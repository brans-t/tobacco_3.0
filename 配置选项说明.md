# ToBaCCo 配置选项说明

本文档说明如何在 `generate_cif()` 函数中使用配置选项，特别是 `USER_SPECIFIED_NODE_ASSIGNMENT` 和 `SCALING_ITERATIONS`。

## 基本用法

```python
from src.api import generate_cif

# 通过 config 参数传递配置选项
config = {
    'USER_SPECIFIED_NODE_ASSIGNMENT': False,
    'SCALING_ITERATIONS': 1,
}

result = generate_cif(
    template_name="pcu",
    node_names="6c_Cu_1_Ch",
    edge_names="1B_1TrU",
    config=config  # 传递配置字典
)
```

## 主要配置选项

### 1. USER_SPECIFIED_NODE_ASSIGNMENT (布尔值)
**默认值:** `False` (全局), `True` (API 模式)

控制在顶点分配时考虑哪些节点：
- `True`: 仅使用 `node_names` 参数中指定的节点
- `False`: 考虑数据库中所有可用的节点

**使用场景:**
- 设置为 `True` 可以精确控制使用哪些节点
- 设置为 `False` 允许算法探索所有兼容的节点

```python
config = {'USER_SPECIFIED_NODE_ASSIGNMENT': True}
```

### 2. SCALING_ITERATIONS (整数)
**默认值:** `1`

单元格优化的迭代次数。

**使用场景:**
- 增加迭代次数（如 2-5）可以获得更好的单元格参数优化
- 更高的值会增加计算时间，但可能改善结果
- 使用 1 可以快速生成

```python
config = {'SCALING_ITERATIONS': 3}  # 更多优化迭代
```

### 3. CHARGES (布尔值)
**默认值:** `True`

启用/禁用原子电荷分配。

```python
config = {'CHARGES': False}  # 禁用电荷
```

### 4. RANDOM_SEED (整数)
**默认值:** `42`

确定性电荷生成的随机种子。相同的种子产生相同的电荷。

```python
config = {'RANDOM_SEED': 123}  # 自定义种子
```

### 5. MIN_CELL_LENGTH (浮点数)
**默认值:** `5.0`

最小单元格长度（埃）。防止单元格塌陷。

```python
config = {'MIN_CELL_LENGTH': 10.0}  # 更大的最小值
```

### 6. REMOVE_DUMMY_ATOMS (布尔值)
**默认值:** `True`

从最终结构中移除虚拟原子（Fr）。

```python
config = {'REMOVE_DUMMY_ATOMS': False}  # 保留虚拟原子
```

## 完整示例

```python
from src.api import generate_cif

# 综合配置
config = {
    # 结构生成
    'USER_SPECIFIED_NODE_ASSIGNMENT': True,
    'SCALING_ITERATIONS': 2,
    
    # 单元格
    'MIN_CELL_LENGTH': 10.0,
    'PRE_SCALE': 1.1,
    
    # 电荷
    'CHARGES': True,
    'RANDOM_SEED': 42,
    
    # 原子和键
    'REMOVE_DUMMY_ATOMS': True,
    'CONNECTION_SITE_BOND_LENGTH': 1.54,
    'BOND_TOL': 3.0,
    
    # 优化
    'OPT_METHOD': 'L-BFGS-B',
    
    # 过滤
    'SINGLE_METAL_MOFS_ONLY': True,
    'MOFS_ONLY': True,
}

result = generate_cif(
    template_name="pcu",
    node_names="6c_Cu_1_Ch",
    edge_names="1B_1TrU",
    config=config
)

print(f"生成: {result['cifname']}")
print(f"原子数: {result['metadata']['num_atoms']}")
print(f"单元格: {result['metadata']['unit_cell_params']}")
```

## 其他配置选项

### 单元格参数
- `FIX_UC` (元组): 固定特定的单元格参数 (a, b, c, alpha, beta, gamma)
- `PRE_SCALE` (浮点数): 优化前应用的预缩放因子

### 优化设置
- `OPT_METHOD` (字符串): 单元格缩放的优化方法（默认: 'L-BFGS-B'）
- `BOND_TOL` (浮点数): 基于距离的成键容差

### 过滤选项
- `SINGLE_METAL_MOFS_ONLY` (布尔值): 仅生成单一金属类型的 MOF
- `MOFS_ONLY` (布尔值): 仅生成包含金属的结构（MOF）
- `COMBINATORIAL_EDGE_ASSIGNMENT` (布尔值): 尝试所有边分配组合

## 示例文件

查看这些示例文件以获取实际演示：

1. **examples/quick_start.py** - 基本用法，包含配置选项
2. **examples/advanced_config_example.py** - 综合配置示例
3. **test_config_options.py** - 配置选项测试套件

## 运行示例

```bash
# 基本示例
python examples/quick_start.py

# 高级配置示例
python examples/advanced_config_example.py

# 测试配置选项
python test_config_options.py
```

## 配置方式对比

### 方式 1: 全局配置文件 (`configuration.py`)
影响所有 MOF 生成，除非被覆盖：

```python
# 在 configuration.py 中
SCALING_ITERATIONS = 3
USER_SPECIFIED_NODE_ASSIGNMENT = True
```

### 方式 2: API 参数（推荐）
为特定生成覆盖配置：

```python
# 在你的脚本中
config = {
    'SCALING_ITERATIONS': 3,
    'USER_SPECIFIED_NODE_ASSIGNMENT': True
}

result = generate_cif(..., config=config)
```

**推荐:** 使用 API 参数以获得灵活性和可重现性。

## 使用建议

1. **从简单开始:** 先使用默认设置，然后根据需要调整
2. **增加迭代次数:** 使用 `SCALING_ITERATIONS: 2-3` 获得更好的优化
3. **控制节点:** 设置 `USER_SPECIFIED_NODE_ASSIGNMENT: True` 以精确控制
4. **可重现性:** 始终设置 `RANDOM_SEED` 以获得可重现的结果
5. **单元格大小:** 如果单元格塌陷，调整 `MIN_CELL_LENGTH`
6. **性能:** 降低 `SCALING_ITERATIONS` 以加快生成速度

## 完整文档

查看以下文档获取更多信息：
- `configuration.py` - 所有选项及默认值
- `examples/CONFIG_OPTIONS.md` - 详细文档（英文）
- `CONFIGURATION_OPTIONS_SUMMARY.md` - 快速参考（英文）
- `CONFIGURATION_GUIDE.md` - 全局配置文件指南

## 常见问题

### 问：如何只使用我指定的节点？
答：设置 `USER_SPECIFIED_NODE_ASSIGNMENT: True`

### 问：如何提高单元格优化质量？
答：增加 `SCALING_ITERATIONS` 的值（如 2-5）

### 问：如何确保每次生成相同的电荷？
答：设置固定的 `RANDOM_SEED` 值

### 问：如何禁用电荷分配？
答：设置 `CHARGES: False`

### 问：如何防止单元格塌陷？
答：增加 `MIN_CELL_LENGTH` 的值

## 技术支持

如有问题，请查看：
- 主 README.md 文件
- examples/README.md 文件
- docs/API_DOCUMENTATION.md 文件
